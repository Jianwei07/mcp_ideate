# 05 React Console

Implement the session sidebar, research composer, live clickable transcript,
sampling approval card, masked details, optional thinking toggle, context and
token metrics, grounded answer, citations, cancellation, and clear errors.

Verification: `cd frontend && bun run build`.

Done when the browser can complete and inspect one full research turn.

Rollback: retain the current Vite UI until final cutover.
